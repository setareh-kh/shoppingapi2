namespace shoppingapi2.Services.Service;
/*UploadAsync(
    IFormFile file,
    string itemType,
    int itemId,
    int priority)

    GetImagesAsync(
    string itemType,
    int itemId)

    DeleteImagesAsync(
    string itemType,
    int itemId)

    POST /api/images
    */